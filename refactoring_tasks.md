# Технический долг и задачи по рефакторингу

Этот документ содержит список найденных ошибок, недоработок и архитектурных проблем, которые требуют исправления.

---

### 🎯 Критические ошибки и баги

- [x] **Некорректная работа запланированных рассылок**
  - **Проблема:** Запланированные рассылки (`scheduled_at`) отправляются немедленно, а не в указанное время.
  - **Задача:**
    1. Изменить эндпоинт `POST /api/v1/broadcasts`, чтобы он только сохранял рассылку со статусом `scheduled` и не вызывал `send_broadcast.delay()` сразу.
    2. Создать новую периодическую задачу Celery Beat (например, `check_scheduled_broadcasts`), которая запускается каждую минуту.
    3. Эта задача должна запрашивать из БД рассылки со статусом `scheduled` и временем `scheduled_at <= NOW()`, менять их статус на `sending` и вызывать для каждой `send_broadcast.delay()`.

- [x] **Отсутствуют критически важные переменные конфигурации**
  - **Проблема:** В `.env.example` и, следовательно, в `settings` отсутствуют переменные, необходимые для аутентификации и работы бота.
  - **Задача:** Добавить в `.env.example` и в класс `Settings` (`app/config.py`) следующие переменные с адекватными значениями по умолчанию:
    - `JWT_SECRET_KEY` (например, сгенерированный через `openssl rand -hex 32`)
    - `JWT_ALGORITHM` (например, `HS256`)
    - `ACCESS_TOKEN_EXPIRE_MINUTES` (например, `60`)
    - `TELEGRAM_CHANNEL_ID` (например, пустая строка или ID канала)

- [x] **Гонка состояний (Race Condition) при погашении скидки**
  - **Проблема:** Два кассира могут одновременно погасить один и тот же код, так как отсутствует блокировка на уровне БД.
  - **Задача:** В обработчике колбэка `redeem_code` (`app/routers/bot_auth.py`) применить блокировку `SELECT ... FOR UPDATE` при запросе скидки из БД, чтобы транзакция ожидала, если другая транзакция уже работает с этой строкой.

---

### 🚀 Производительность и масштабируемость

- [x] **Отсутствуют индексы для внешних ключей в БД**
  - **Проблема:** Большинство столбцов с внешними ключами не индексированы, что приведет к катастрофическому падению производительности.
  - **Задача:** Создать новую миграцию Alembic, которая добавляет индексы (`index=True`) для всех столбцов, являющихся внешними ключами, в следующих таблицах: `audit_logs`, `broadcast_logs`, `campaigns`, `cashiers`, `discounts`, `orders`, `campaign_users`, `discount_usage_logs`.

- [x] **Блокирующие вызовы в асинхронном коде**
  - **Проблема:** Во всем проекте `async` функции (эндпоинты API, обработчики ботов) используют синхронные вызовы к БД, блокируя event loop.
  - **Задача:** Провести рефакторинг. Рассмотреть один из двух подходов:
    1. **(Предпочтительный)** Заменить драйвер БД `pymysql` на асинхронный аналог (например, `asyncmy`) и переписать все обращения к БД с использованием `await db.execute(...)`.
    2. **(Альтернативный)** Преобразовать все `async def` эндпоинты и обработчики, работающие с БД, в обычные `def`, сделав их синхронными. FastAPI и Aiogram поддерживают это, но теряются преимущества асинхронности для I/O операций.

- [x] **Избыточные операции записи в БД при каждом сообщении**
  - **Проблема:** Функция `_get_or_create_user` в `app/routers/bot_main.py` обновляет данные пользователя (имя, юзернейм) при каждом входящем сообщении, создавая лишнюю нагрузку.
  - **Задача:** Изменить логику `_get_or_create_user`. Обновление данных пользователя должно происходить только если они действительно изменились, а не при каждом вызове.

---

### 🛠️ Качество кода и надежность

- [x] **Улучшить парсер даты рождения**
  - **Проблема:** Парсер в `app/routers/bot_main.py` слишком примитивен, негибок к форматам и не валидирует дату.
  - **Задача:** Переписать функцию `_parse_birthday`, добавив:
    - Поддержку разных порядков (Д.М.Г, Г.М.Д).
    - Валидацию на корректность (например, `birthday < today`).
    - Валидацию на разумный диапазон года (например, `1920 < year < today.year - 14`).

- [x] **Дублирование логики валидации скидки**
  - **Проблема:** В `app/routers/bot_auth.py` логика проверки кода скидки дублируется в двух местах.
  - **Задача:** Создать единую вспомогательную функцию `_validate_discount(db, code)`, которая будет инкапсулировать всю логику проверок и возвращать либо объект скидки, либо причину ошибки.

- [x] **Небезопасное обновление настроек**
  - **Проблема:** Эндпоинт `PATCH /api/v1/settings` позволяет записать в любую настройку значение любого типа, что может привести к ошибкам в других частях системы.
  - **Задача:** Внедрить Pydantic-схему для валидации типов значений при обновлении настроек.

- [x] **Улучшить обработку ошибок в боте**
  - **Проблема:** При ошибках API (например, недоступен Telegram) бот просто молчит.
  - **Задача:** В обработчиках сообщений (`app/routers/bot_main.py` и др.) добавить блоки `try...except TelegramAPIError` и отправлять пользователю сообщение об ошибке (например, "Не удалось выполнить операцию, попробуйте позже").

- [x] **Устранить "магические" значения**
  - **Проблема:** В коде жестко зашиты константы.
  - **Задача:** Вынести хардкод-значения (время cron, `30` дней для скидки, префикс `КОД`) в файл конфигурации `settings` или в виде констант в начале соответствующего модуля.

- [x] **Пересмотреть права на активацию кассиров**
  - **Проблема:** Роль `marketing` может активировать кассиров, что может быть слишком высокой привилегией.
  - **Задача:** В `app/routers/cashiers.py` изменить требуемую роль для эндпоинтов `activate` и `deactivate` с `marketing` на `owner`.
